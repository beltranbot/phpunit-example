version: "3.8"

services:
    php:
        build:
            context: ./docker/php
            dockerfile: Dockerfile
        ports:
            - "80:80"
        volumes:
            - .:/var/www/html
